SkyPro Music Next
Учебное приложение для изучения Next.js с использованием TypeScript.

🚀 Технологии
Frontend: Next.js, TypeScript, CSS

<H1>📦 Установка и запуск bash</H1>
<H4> Клонирование репозитория </H4>
git clone https://github.com/your-username/skypro-music-next.git
cd skypro-music-next

<H4>Установка зависимостей</H4>

npm install

<H4>Запуск в режиме разработки</H4>

npm run dev

<H4>Сборка для production</H4>

npm run build

<H4>Запуск production сборки</H4>

npm start

🛠 Основные команды Git для работы с проектом
Начало работы bash

<H4>Клонирование реpository</H4>

git clone https://github.com/your-username/skypro-music-next.git

<H4>Переход в директорию проекта</H4>

cd skypro-music-next

# Создание своей ветки для разработки

git checkout -b feature/your-feature-name

Ежедневный workflow bash

# Проверка статуса изменений

git status

# Добавление измененных файлов

git add . # Все файлы
git add src/components/Button.tsx # Конкретный файл

# Создание коммита

git commit -m "feat: add audio player component"

# Получение последних изменений с удаленного репозитория

git pull origin main

# Отправка изменений в удаленный репозиторий

git push origin feature/your-feature-name

Ветвление и слияние bash

# Создание новой ветки

git branch new-feature-branch
git checkout new-feature-branch

# Или одной командой:

git checkout -b new-feature-branch

# Переключение между ветками

git checkout main
git checkout develop
git checkout feature-branch

# Слияние веток

git checkout main
git merge feature-branch

# Удаление локальной ветки

git branch -d feature-branch

# Удаление удаленной ветки

git push origin --delete feature-branch

Откат изменений bash

# Отмена непроиндексированных изменений в файле

git checkout -- filename.ts

# Отмена всех непроиндексированных изменений

git checkout -- .

# Отмена индексированных изменений (перед коммитом)

git reset HEAD filename.ts

# Отмена коммита (сохраняет изменения в рабочей директории)

git reset --soft HEAD~1

# Полный откат коммита (удаляет изменения)

git reset --hard HEAD~1

# Откат конкретного коммита

git revert COMMIT_HASH

Работа с историей bash

# Просмотр истории коммитов

git log --oneline
git log --graph --pretty=format:'%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'

# Просмотр изменений в файлах

git diff
git diff filename.ts

# Просмотр кто менял файл

git blame filename.ts
Работа с тегами
bash

# Создание тега для версии

git tag v1.0.0
git push origin v1.0.0

# Просмотр всех тегов

git tag

🏗 Структура проекта text
skypro-music-next/
├── src/
│ ├── app/ # Next.js 13+ app directory
│ ├── components/ # React компоненты
│ ├── lib/ # Утилиты и конфигурации
│ ├── types/ # TypeScript типы
│ └── utils/ # Вспомогательные функции
├── public/ # Статические файлы
├── prisma/ # Схема базы данных
└── docs/ # Документация
